# How to use the application and run tests
The application does not use password so you just put in the user initials the access the app (e.g. huba).
As for running the app you have to disable module-info.